!git clone https://github.com/GaParmar/img2img-turbo.git

!pip install -q condacolab
import condacolab
condacolab.install()

import os
os.chdir('/content/img2img-turbo')

!conda env create -f environment.yaml

!python3 -m venv venv
!source venv/bin/activate
!pip install -r requirements.txt

!pip install huggingface_hub==0.23.0

for num in range(1, 121):
    input_image = f"../images{num}.jpeg"
    output_dir = f"outputs{num}"
    !python src/inference_unpaired.py --model_name "night_to_day" --input_image "{input_image}" --output_dir "{output_dir}"

from google.colab import drive
drive.mount('/content/drive')
